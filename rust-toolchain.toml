# TEMPORARY PIN — see NIGHTLY_PIN.md.
#
# Pinned to the last nightly before rust-lang/rust#158009 (2026-06-17, oli-obk)
# removed `impl const Trait` in favor of `const impl Trait`. The c0nst 0.2.1
# macro still emits the old `impl const Trait`, so any newer nightly fails the
# `nightly`/`nightly-std` features with "expected a trait, found type".
#
#   last good: nightly-2026-06-18  (rustc 1.98.0-nightly c1b22f44c, 2026-06-17)
#   first bad: nightly-2026-06-19  (rustc 1.98.0-nightly f428d123a)
#   rust PR:   https://github.com/rust-lang/rust/pull/158009
#
# Unpin once c0nst emits `const impl Trait`, or once our source migrates from
# `impl c0nst Trait` to `c0nst impl Trait`. See NIGHTLY_PIN.md.
[toolchain]
channel = "nightly-2026-06-18"
